[package]
name = "apex-hot-reload"
version.workspace = true
edition.workspace = true

[dependencies]
apex-core          = { path = "../apex-core" }
apex-serialization = { path = "../apex-serialization" }

# File watcher — cross-platform.
# notify 6.x picks the OS backend automatically via cfg:
#   Windows → ReadDirectoryChangesWatcher
#   Linux   → INotifyWatcher
#   macOS   → KqueueWatcher / FSEventsWatcher
# No extra feature flags are needed.
notify = "6.1"

serde      = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rustc-hash.workspace = true
thiserror.workspace  = true
log.workspace        = true

[dev-dependencies]
# Tests write to disk only through a directory their Drop removes (engine TD-564).
apex-test-dir = { path = "../apex-test-dir" }
